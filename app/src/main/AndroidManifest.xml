<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="callmedj.com.accountforlazy" >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <meta-data
        android:name="AA_DB_NAME"
        android:value="FINCIAL.db" />
    <meta-data
        android:name="AA_DB_VERSION"
        android:value="1.0.0" />

    <application
        android:name="com.callmedj.accountforlazy.application.AccountApplication"
        android:allowBackup="true"
        android:icon="@drawable/app_icon"
        android:label="@string/app_name"
        android:theme="@style/Theme.AppCompat" >
        <activity
            android:name="com.callmedj.accountforlazy.counter1.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!--
        <service android:name="com.callmedj.accountforlazy.service.MyAlarmService"></service>
        <service android:name="com.callmedj.accountforlazy.service.NotificationService"></service>
        <receiver
            android:name="com.callmedj.accountforlazy.boradcast.NotificationBoradcast">
            <intent-filter >
                <action android:name="SHOW_NOTIC" />
            </intent-filter>
        </receiver>
        -->
        <activity
            android:name="com.callmedj.accountforlazy.counter1.BaseActivity"
            android:label="@string/title_activity_base" >
        </activity>

    </application>

</manifest>
